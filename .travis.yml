language: node_js

node_js:
  - 8.11.3

cache: yarn

install:
  - yarn install
  - yarn transpile

script:
  - COVERALLS_REPO_TOKEN=$coveralls_repo_token npm run coveralls

deploy:
  provider: npm
  email: ponciusz@gmail.com
  api_key: $NPM_TOKEN
  on:
    tags: true
